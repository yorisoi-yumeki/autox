# CLAUDE.md

このリポジトリで作業するすべてのセッション(ローカル・クラウド/リモート実行環境
問わず)が読む前提のメモ。autoxはXアカウント運用支援CLIで、投稿・DM送信は
一切自動化しない(必ず本人が手動で行う)。

## セッション開始時に必ず確認すること

`config/profile.yaml`・`config/settings.yaml`・`data/autox.db`(下書きキュー・
分析スナップショット)は個人情報のため `.gitignore` 対象で、Gitでは一切運ばれない。

**クラウド/リモート実行環境では、同じ environment_id であっても、セッションを
新規に開始するたびにリポジトリがまっさらに再クローンされるだけで、前回セッションの
これらのファイルは一切引き継がれない。** これは `/clear` によるコンテキストリセット
とは別の問題(ディスクの有無そのものが変わる)なので混同しないこと。

1. 作業を始める前に `config/profile.yaml` と `config/settings.yaml` の存在を確認する。
2. 無ければ、いきなり「データが空なので新規に作ります」と進めない。ユーザーに
   直前セッションの `handoff_bundle.yaml`(下記コマンドで作れる)を貼ってもらい、
   `autox handoff import <path>` で復元してから続ける。
3. 存在する場合も、下書きキューの状況は要約や記憶に頼らず
   `autox content list --status draft` 等で実データから確認する。

## セッション終了時・コンテキストが肥大化してきた時

ユーザーから「引き継ぎを作って」「次のセッションへの指示を考えて」等と言われたら、
**要約文だけを作って終わらない。** 必ず次の手順で、新しいチャットにそのまま貼り付けて
使える1つのテキストブロックを作ること。

1. `autox handoff export` を実行し、`data/handoff_bundle.yaml` を作る。
2. そのファイルの中身をそのまま読み込む。
3. 以下のテンプレートに当てはめて出力する(コードブロックでまとめて、
   ユーザーがそのままコピーできる形にする):

   ```
   autox (yorisoi-yumeki/autox) の続きから作業してください。ブランチ: <現在のブランチ名>

   ## 最初にやること(必須)
   1. CLAUDE.md を読んでルールを把握する
   2. config/profile.yaml が存在するか確認する。無ければ(=新しいクラウド環境):
      a. 下記「復元用バンドル」の内容をそのまま `data/handoff_bundle.yaml` として保存する
      b. `autox handoff import data/handoff_bundle.yaml` を実行して復元する
   3. `autox content list --status draft` 等で下書きキューの状況を実データから確認する
      (このテキストの要約や記憶に頼らない)

   ## 復元用バンドル
   ```yaml
   <ここに autox handoff export の出力(data/handoff_bundle.yaml の中身)をそのまま貼る>
   ```

   ## 今どこまで進んでいるか / 次にやること
   <直近のやり取りを踏まえた、具体的な次のアクション。2〜5行程度。
   「〜について検討した」のような曖昧な経緯要約ではなく、
   「〜を確認する」「〜を実行する」のような次の一手として書く>
   ```

**「これまでの経緯を要約する」のではなく、実データそのものを引き継ぐこと。**
自然言語の要約だけに頼ると、微妙なニュアンスの重複や個別の回答内容が抜け落ちる
リスクがある(実際に過去、下書きキューへの言及漏れが起きたことがある)。
「次にやること」の部分だけは短い自然言語でよいが、データ本体(復元用バンドル)は
必ず実データをそのまま貼ること。

## 投稿文を書く際の傾向・ルール(profile.yaml/settings.yamlの個別回答とは別に、常に厳守)

- 記録済みの事実だけを使う。エピソードや例え話を勝手に捏造しない
- 自分の欠点を晒す投稿は不要(ギャップ系ユーモアはOK、弱点の告白はNG)
- 愚痴・dealbreaker系のネガティブな内容は避け、「会うと楽しそう」と思わせる
  ポジティブな内容中心
- 同じニュアンス・フレーズの重複を投稿間で避ける(下書きキュー内・過去投稿と
  照らし合わせる)
- 「〜でしょ😂」のような自画自賛的な言い回しは不自然なので避ける
- 「放置される」「待つ」のような受け身な言い回しより、本人の実際の反応
  (冷める・興味が切れる等)に忠実に書く
