# このファイルをコピーして config/settings.yaml を作り、中身を自分用に編集してください。
#   cp config/settings.example.yaml config/settings.yaml
# config/settings.yaml は .gitignore 対象なのでリポジトリにはコミットされません。

# 投稿のトーン(生成AIへの指示のベースになります)
tone: |
  誠実で気取らない、少し丁寧語混じりの話し方。誇張や見栄を張らない。
  相手を落とすためのテクニックではなく、素の考えを共有する姿勢で書く。
  愚痴・不満・dealbreaker系のネガティブな内容は避け、ポジティブな内容を多めにする
  (読んだ人が「会ったら楽しそう」と感じることを目指す)。

# (任意) 他サービスの自己紹介文など、自分の実際の文体・ユーモアのクセが
# よく出ている文章があれば、参考として貼っておくと生成の精度が上がる。
# 内容(固有名詞やエピソード)をそのままXの投稿にコピーするためのものではなく、
# あくまで「話し方の参考」としてAIに渡される。不要なら削除してよい。
# voice_reference: |
#   (例: マッチングアプリの自己紹介文、ブログの一節、など)

# 投稿の柱(コンテンツカテゴリ)。ここに書いたもの以外は生成しない。
content_pillars:
  - key: relationship_values
    label: 恋愛観
    description: 恋愛や人間関係で大事にしていること、最近考えていること
  - key: daily_life
    label: 日常
    description: 仕事終わりの過ごし方、休日の出来事など、事実ベースの日常
  - key: hobby
    label: 趣味
    description: 趣味・好きなものについて語る投稿
  - key: work_values
    label: 仕事観
    description: 仕事に対するスタンスや考え方(具体的な勤務先/個人情報は書かない)

# 投稿するゴールデンタイム帯(この範囲内でランダムな時刻に予約する)
golden_hour:
  start: "20:00"
  end: "22:00"
  timezone: "Asia/Tokyo"

# DM下書き生成の際に踏まえる、自分がどんな相手と繋がりたいか
matching_criteria: |
  価値観が近く、誠実にやり取りできる相手。
  下心だけでなく、対話を大事にしてくれる人。

# 運用を開始した日(分析レポートで「開始から○ヶ月経過」を計算するために使う)。
# 終了予定日ではない。長期的に続ける前提の場合でも、経過日数の目安として設定しておく。
started_on: "2026-01-01"
