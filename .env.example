# Claude(Anthropic API)で投稿文/DM返信下書きを生成する場合のみ必要。
# 未設定でも動作する(生成せず data/prompts/ にプロンプトを書き出すだけになる)。
ANTHROPIC_API_KEY=

# config/ 以下を別ディレクトリに置きたい場合のみ上書き(通常は不要)
# AUTOX_CONFIG_DIR=config
# AUTOX_DATA_DIR=data
