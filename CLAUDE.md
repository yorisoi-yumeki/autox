# autox 作業メモ(Claude向け)

X運用支援CLI。詳しい使い方は `README.md`、方針は `docs/account_strategy.md` /
`docs/compliance_notes.md` を参照。ここには、コードを読んだだけでは分からない
**運用上の取り決め**だけを書く。

## 投稿文を書く/添削するときの厳守事項

過去のセッションで本人から出た指示。生成AIに任せるかどうかに関わらず、
`autox content add` で手動登録する下書きにも同じ基準を適用する。

1. **記録済みの事実だけを使う。** `config/profile.yaml` に無いエピソード・比喩・
   芸能人名などを勝手に作らない。「それっぽい具体例」の創作が一番嫌われる。
2. **自分の欠点を晒す投稿は作らない。** 第一印象とのギャップ系ユーモアは可。
   弱点・短所の告白はNG(例: 夜更かし癖のような `personality_growth_point` 系は
   投稿にしない)。
3. **同じフレーズ・同じニュアンスの重複を避ける。** 新しい下書きを出す前に
   `autox content list` で既存分を確認する。
4. **自画自賛的な言い回しを使わない。** 例:「〜でしょ😂」「正直者でステキ!でしょ」。
5. **意味の通らない曖昧な締めの一文を付けない。** 語呂で締めない。

同じ内容を `config/settings.yaml` の `tone` にも書いてあるので、AI生成時は
システムプロンプト経由でも渡る。片方だけ直さないこと。

## スケジューリング時の注意

`autox schedule run` は承認済み下書きを1日1件ずつ順に割り当てるだけで、
**内容の重複検知は実装されていない**。似た趣旨の下書き(例: ギャップ/健康ネタ)が
複数あるときは、同日・近接日に並ばないよう承認する順序や日付を手で調整する。

## ローカル専用データ(gitignore対象)

新しい環境では以下が存在しない。本人に再作成してもらうか、手元から持ち込む。

| パス | 中身 | 無いとどうなるか |
|---|---|---|
| `config/settings.yaml` | トーン・投稿の柱・voice_reference | `settings.example.yaml` にフォールバックするため、本人の文体が反映されない |
| `config/profile.yaml` | 価値観インタビューの回答 | 生成が「一般的なトーン」になる。`autox profile interview` で再入力できる |
| `data/autox.db` | 下書きキュー・分析スナップショット | 下書きが全て消える。`autox content add` で再登録が必要 |
| `.env` | `ANTHROPIC_API_KEY` | 未設定でも動く。生成系コマンドは `data/prompts/` にプロンプトを書き出すだけになる |

`data/prompts/*.txt` はAPIキー未設定時のフォールバック出力なので、消してよい。
